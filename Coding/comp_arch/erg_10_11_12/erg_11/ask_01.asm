	.data 
msg1:  .asciiz "Give base "
msg2: .asciiz "Give height "
msg3: .asciiz "Triangle's area is "
	.text
	
	addiu $v0, $zero, 4
	la $a0, msg1 		# Τοποθέτηση ο�?ίσματος
	syscall 			# Κλήση συστήματος
	addi $v0, $zero, 6  # Υπη�?εσία 6 (διάβασμα π�?αγματικο�? Κ.Υ. α.α.)
	syscall 			# Κλήση συστήματος
	mov.s $f20, $f0 	# Μεταφο�?ά του π�?αγματικο�? στο $f20
	addiu $v0, $zero, 4 # Υπη�?εσία 4 (εμφάνιση μην�?ματος)
	la $a0, msg2 		# Τοποθέτηση ο�?ίσματος
	syscall 			# Κλήση συστήματος
	addi $v0, $zero, 6 	# Υπη�?εσία 6 (διάβασμα π�?αγματικο�? Κ.Υ. α.α.)
	syscall 			# Κλήση συστήματος
	mov.s $f21, $f0 	# Μεταφο�?ά του π�?αγματικο�? στο $f21
	mul.s $f22, $f20, $f21 	# Πολλαπλασιασμός: $f22 <- $f20 x $f21
	addi $t0, $zero, 2 		# $t0 <- 2
	mtc1 $t0, $f4 		# $f4 <- 2 (Μεταφο�?ά από το $t0 CPU στο $f4 FPU)
	cvt.s.w $f4, $f4	# $f4 <- 2.0 (Μετατ�?οπή από ακέ�?αιο σε π�?αγματικό Κ.Υ. α.α.)
	div.s $f22, $f22, $f4 	# Διαί�?εση διά 2.0
	addiu $v0, $zero, 4 	# Υπη�?εσία 4 (εμφάνιση μην�?ματος)
	la $a0, msg3 		# Τοποθέτηση ο�?ίσματος
	syscall 			# Κλήση συστήματος
	addi $v0, $zero, 2 	# Υπη�?εσία 2 (εμφάνιση π�?αγματικο�? Κ.Υ. α.α.)
	mov.s $f12, $f22 	# Τοποθέτηση ο�?ίσματος
	syscall 			# Κλήση συστήματος
